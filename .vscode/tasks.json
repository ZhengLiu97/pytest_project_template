{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "echo",
        "type": "shell",
        "command": "echo ${workspaceFolder}"  // make sure the workspaceFolder value is as expected
      }
    ]
  }